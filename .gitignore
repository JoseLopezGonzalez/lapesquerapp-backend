/.phpunit.cache
/node_modules
/public/build
/public/hot
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
vendor/

# Memoria de trabajo de agentes IA (sesiones temporales)
.ai_work_context/

# Scribe (cache/temporal)
/.scribe/
# Documentación API generada (solo local/CI; no desplegar en producción)
/public/docs/

# Cobertura de tests (generada por phpunit --coverage)
/coverage/
