/.phpunit.cache
/node_modules
/public/build
/public/hot
/storage/*.key
/vendor
.env
.env.local
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
vendor/

# Memoria de trabajo de agentes IA (sesiones temporales)
.ai_work_context/

# Scribe (cache/temporal)
/.scribe/
/.scribe_public/
# Documentación API interna/completa generada (solo local/CI; no desplegar en producción,
# incluye superadmin). El contrato frontend SÍ se versiona: ver /public/openapi/ más abajo.
/public/docs/
# Salida de trabajo intermedia de la config Scribe "frontend" (contract:publish/contract:check
# la genera aquí y copia solo openapi.yaml a /public/openapi/frontend.yaml, que sí se versiona).
/storage/app/scribe-public/

# Cobertura de tests (generada por phpunit --coverage)
/coverage/
