APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
# URL del frontend para magic link. Usa {subdomain} para sustituir por el tenant (brisamar, pymcolora, etc.)
FRONTEND_URL=https://{subdomain}.lapesquerapp.es
# Opcional: minutos de validez del magic link/OTP (por defecto 10)
# MAGIC_LINK_EXPIRES_MINUTES=10
# Opcional: días de antigüedad para borrar tokens ya usados en la limpieza (0 = no borrar por antigüedad)
# MAGIC_LINK_CLEANUP_USED_DAYS=1

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=94.143.137.84
DB_PORT=3308
DB_DATABASE=default  
DB_USERNAME=root
DB_PASSWORD='PFkXCoswvTzsr8f5YNVK7lDOCtfyRNUOoVjjpQGb8a6orP4uopB9RP4xVydZjeFK'

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# -----------------------------------------------------------------------------
# CORS y Sanctum (multi-tenant SPA)
# -----------------------------------------------------------------------------
# Orígenes permitidos (comma-separated). En producción: lista blanca específica.
# Los subdominios *.lapesquerapp.es y *.congeladosbrisamar.es se permiten vía patrones en config/cors.php
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001,http://localhost:5173

# Dominios que reciben cookies de Sanctum (SPA stateful). Sin protocolo.
# Producción: subdominio.lapesquerapp.es,lapesquerapp.es
SANCTUM_STATEFUL_DOMAINS=localhost:3000,localhost:3001,127.0.0.1:3000,127.0.0.1:3001

# SESSION_DOMAIN para producción (opcional): .lapesquerapp.es para compartir cookie entre subdominios
# SESSION_DOMAIN=.lapesquerapp.es

# -----------------------------------------------------------------------------
# Variables para desarrollo con Docker Sail (opcional)
# Para usar Sail: cp .env.sail.example .env y ajustar, o fusionar estas claves.
# -----------------------------------------------------------------------------
# DB_HOST=mysql
# DB_PORT=3306
# DB_DATABASE=pesquerapp
# DB_USERNAME=sail
# DB_PASSWORD=password
# REDIS_HOST=redis
# REDIS_PORT=6379
# CACHE_DRIVER=redis
# QUEUE_CONNECTION=redis
# SESSION_DRIVER=redis
# SANCTUM_STATEFUL_DOMAINS=localhost:3000,127.0.0.1:3000
# MAIL_FROM_ADDRESS="noreply@pesquerapp.local"
